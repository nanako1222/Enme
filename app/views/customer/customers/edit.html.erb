<div class="container">
  <div class="row">
    <div class="col-10 offset-1">
      <h4 class="col-md-3 mt-5">会員情報編集</h4>

      <div class='flashing mt-2'>
        <p>※現在登録されているデータは架空です</p>
      </div>

      <div class="mt-4">
        <%= form_with model: @customer, url: customers_information_path, method: :patch do |f| %>
          <div class="row mt-3">
            <%= f.label :氏名, class: "col-md-3 text-left" %>
            <%= f.text_field :first_name, autofocus: true, required: true, class: "col-md-3" %>
            <%= f.text_field :last_name, autofocus: true, required: true, class: "col-md-3 ml-2" %>
          </div>

          <div class="row mt-4">
            <%= f.label :フリガナ, class: "col-md-3 text-left" %>
            <%= f.text_field :first_name_kana, autofocus: true, required: true, class: "col-md-3" %>
            <%= f.text_field :last_name_kana, autofocus: true, required: true, class: "col-md-3 ml-2" %>
          </div>

          <div class="row mt-4">
            <%= f.label :メールアドレス, class: "col-md-3 text-left" %>
            <%= f.email_field :email, autofocus: true, class: "col-md-6" %>
          </div>

          <div class="row mt-4">
            <%= f.label :都道府県, class: "col-md-3 text-left" %>
            <%= f.collection_select :state_id , State.all,:id,:state %>
          </div>

          <div class="row mt-3">
            <%= f.label :地域エリア, class: "col-md-3 text-left" %>
            <%= render partial: 'shared/areas', locals: { areas: Area.all } %>
          </div>

          <p class="offset-md-3 mt-2">
            ※エリア分け詳細は
            <%= link_to "こちら", about_path, class: "btn btn-outline-info btn-sm", data: {"turbolinks" => false} %>
            をご参考ください
          </p>

          <div class="row mt-4">
            <%= f.label :電話番号（ハイフンなし）, class: "col-md-3 text-left" %>
            <%= f.number_field :telephone_number, autofocus: true, class: "col-md-6" %>
          </div>

          <div class="row mt-3">
            <%= f.label :アレルゲン情報登録, class: "col-md-3 text-left" %>
            <%#= f.collection_check_boxes :allergy_ids, @allergies, :id, :allergen, include_hidden: false %>
            <div class="allergies_check_boxes col-md-7">
              <%= f.collection_check_boxes(:allergies, @allergies, :id, :allergen, include_hidden: false) do |a| %>
                <%= a.label { a.check_box + a.text } %>
              <% end %>
            </div>
          </div>

          <div class="row field d-grid offset-md-3">
            <%= button_tag type: :submit, class: "btn btn-success active mt-4" do %>
              <i class="fas fa-save"></i> 編集内容を保存
            <% end %>
            <%= link_to customers_confirm_path(@customer), class:"btn btn-danger active ml-md-5 mt-4" do %>
              <i class="fas fa-user-slash"></i>
              退会する
            <% end %>
          </div>
        <% end %>


      </div>
    </div>
  </div>
</div>