<div class="container">
  <div class="row">
    <div class="col-12">
      <div class='flashing mt-5 offset-2'>
        <p>※現在登録されているデータは架空です</p>
      </div>
      <div class="row mt-5">
        <div class="col-md-5 mt-5 text-center">
          <div class="menu_image">
            <% if @menu.image.attached? %>
              <%= image_tag @menu.get_image(200,200) %>
            <% else %>
              <%= image_tag 'default-image.jpg', size: "200x200" %>
            <% end %>
          </div>
        </div>

        <div class="col-md-7 mt-5 menu_show">
          <h5><strong><%= @menu.name %></strong></h5>
          <div class="mt-3"><%= @menu.introduction %></div>
          <div class="mt-3">
            <p class="menu_allergen"><strong>含まれるアレルゲン</strong></p>
            <% @menu.menu_having_allergies.each do |e| %>
              <td><%= e.allergy.allergen %></td>
            <% end %>
          </div>
            <!--<th>税込価格</th>-->
          <div class="mt-5"><span>税抜価格（税込価格）</span><br><strong class="price"><%= "#{@menu.price}円" %>(<%= "#{@menu.with_tax_price}円" %>)　</strong></div>
        </div>

        <div class = "mt-4 offset-4">
          <%= link_to restaurant_menus_path(@restaurant), class:"btn btn-info active btn-sm ml-3" do %>
            <i class="fas fa-clipboard-list"></i>
            メニュー一覧に戻る
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>